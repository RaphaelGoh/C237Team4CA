<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Product | Gangnam Spice</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body {
      font-family: 'Noto Sans KR', sans-serif;
      background-color: #fff5f5;
    }
    .form-container {
      max-width: 600px;
      margin: 60px auto;
    }
    .btn-dark-red {
      background-color: #8B0000;
      color: white;
      border: none;
    }
    .btn-dark-red:hover {
      background-color: #600000;
    }
    h2 {
      color: #8B0000;
    }
    .card {
      border-radius: 15px;
    }
  </style>
</head>
<body>

<div class="container form-container">
  <div class="card shadow p-4">
    <a href="/admin-dashboard" class="btn btn-outline-secondary mb-3">← Back to Dashboard</a>
    <h2 class="text-center mb-4">Edit Product</h2>

    <form action="/editProduct/<%= product.productId %>" method="POST" enctype="multipart/form-data">

      <div class="mb-3">
        <label for="name" class="form-label">Product Name</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= product.productName %>" required>
      </div>

      <div class="mb-3">
        <label for="quantity" class="form-label">Quantity</label>
        <input type="number" class="form-control" id="quantity" name="quantity" min="0" step="1" value="<%= product.quantity %>" required>
      </div>

      <div class="mb-3">
        <label for="price" class="form-label">Price (₩)</label>
        <input type="number" class="form-control" id="price" name="price" min="0" step="0.01" value="<%= product.price %>" required>
      </div>

      <div class="mb-3">
        <label for="category" class="form-label">Category</label>
        <select class="form-select" id="category" name="category" required>
          <option value="">-- Select Category --</option>
          <option value="sides" <%= product.category === 'sides' ? 'selected' : '' %>>Sides</option>
          <option value="main-course" <%= product.category === 'main-course' ? 'selected' : '' %>>Main Course</option>
          <option value="drinks" <%= product.category === 'drinks' ? 'selected' : '' %>>Drinks</option>
          <option value="desserts" <%= product.category === 'desserts' ? 'selected' : '' %>>Desserts</option>
          <option value="setmeals" <%= product.category === 'setmeals' ? 'selected' : '' %>>Set Meal</option>
        </select>
      </div>

      <div class="mb-4">
        <label for="image" class="form-label">Update Product Image</label>
        <input class="form-control" type="file" id="image" name="image" accept="image/*">
        <div class="form-text">Leave blank to keep the current image.</div>
      </div>

      <div class="d-grid">
        <button type="submit" class="btn btn-dark-red">Save Changes</button>
      </div>

    </form>
  </div>
</div>

</body>
</html>
